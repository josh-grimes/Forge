# Forge maintenance

For every app change, update both the Google Apps Script version (`Index.html` and `Code.gs` where relevant) and the local version (`Local-Forge-Export/index.html`, `script.js`, and `style.css` where relevant). Keep their shared UI and behavior consistent. Updating source files does not deploy the Google Apps Script web app; report deployment status accurately.
